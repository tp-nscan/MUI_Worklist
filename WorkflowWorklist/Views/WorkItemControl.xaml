<UserControl x:Class="WorkflowWorklist.Views.WorkItemControl"
             x:Name="ControlRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:WorkflowWorklist.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid DataContext="{Binding ElementName=ControlRoot, Path=WorkItemController}" >
        
        <!--<TextBlock Text="{Binding WorkItemMonitorVm.WorkItemStatus }"></TextBlock>-->

        <views:WorkItemMonitorControl x:Name="WorkItemMonitorControl" DataContext="{Binding WorkItemMonitorVm}" />

    </Grid>
    
</UserControl>
